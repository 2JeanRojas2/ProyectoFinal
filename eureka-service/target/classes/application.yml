spring:
  application:
    name: eureka-service

  cloud:
    config:
      discovery:
        enable: true
        id-service: config-service

server:
  port: 8761

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false

